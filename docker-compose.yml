services:
  build-app:
    build: .
    volumes:
      - ./build_output:/app/build_output
    env_file:
      - .env
    working_dir: /app
    command: >
      sh -c '
      flutter pub get &&
      flutter build apk &&
      cp -r build/app/outputs /app/build_output/
      '