import 'package:flutter/material.dart';

/// Источник цветов приложения. Временный — подлежит замене на ColorScheme (Phase 5).
class Palette {
  Color get backgroundMain => const Color(0xffffffff);

  Color get textColor => const Color(0xff333333);

  Color get btnOkColor => const Color(0xff333333);

  Color get tabSelectColor => const Color(0xffff0000);

  Color get tabUnSelectColor => const Color(0xff003cff);

  Color get outline => const Color(0xffffffff);

  Color get primary => const Color(0xffff0000);
}
